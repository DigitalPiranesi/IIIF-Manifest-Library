import EnumManifestBehavior from "./types/enums/EnumManifestBehavior";

/**
 * TODO:
 *  Enums are an essential function of the IIIF presentation API. It would be
 *  useful to produce the correct representation depending on the type of item.
 *  I am not sure exactly what this will look like as we go into the future, but
 *  I have created this class to help with whatever we need it for.
 */
export default class EnumFactory {
}
